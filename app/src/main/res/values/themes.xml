<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.Juyem" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash Screen -->
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.Juyem.Main</item>
    </style>

    <!-- Theme after splash screen -->
    <style name="Theme.Juyem.Main" parent="android:Theme.Material.Light.NoActionBar" />

</resources>